# Use the DAPLink (CMSIS-DAP) interface
source [find interface/cmsis-dap.cfg]

# Select SWD as the transport protocol
transport select swd

# Load the configuration for the STM32G4 family
source [find target/stm32g4x.cfg]